<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@null</item>
        <item name="colorAccent">@color/dimgray</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PassEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/dimgray</item>
        <item name="colorControlActivated">@color/dimgray</item>
        <item name="android:background">@drawable/password_input</item>
    </style>

</resources>
